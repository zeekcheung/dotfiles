#!/usr/bin/env bash

# shellcheck disable=SC1091,SC2154

source "$(dirname "$0")/de_info"

desktop_type="${de_info[DESKTOP_TYPE]}"

if echo "$desktop_type" | grep -qi "gnome"; then
	echo "Current desktop environment is Gnome, restoring dconf settings..."
	bash "$(dirname "$0")/dconf_restore"
fi
